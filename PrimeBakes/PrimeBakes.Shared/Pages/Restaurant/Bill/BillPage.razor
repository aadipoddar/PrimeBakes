@using PrimeBakes.Library.Accounts.Masters.Models
@using PrimeBakes.Library.Operations.Location
@using PrimeBakes.Library.Restaurant.Bill.Models
@using PrimeBakes.Library.Restaurant.Dining.Models
@using PrimeBakes.Library.Store.PaymentMode
@using PrimeBakes.Library.Store.Product.Models

@attribute [Route(RestaurantRouteNames.Bill)]
@attribute [Route($"{RestaurantRouteNames.Bill}/table/{{DiningTableId:int}}")]
@attribute [Route($"{RestaurantRouteNames.Bill}/{{Id:int}}")]

<PageTitle>Bill - Prime Bakes</PageTitle>

<MudBlazor.MudHotkey Key=JsKey.KeyN KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="ResetPage" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyF KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => _firstFocus?.FocusAsync()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyG KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => _itemAutoComplete?.FocusAsync()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyF KeyModifiers="[JsKeyModifier.ControlLeft, JsKeyModifier.ShiftLeft]" OnHotkeyPressed="() => _discountPercentField?.FocusAsync()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyG KeyModifiers="[JsKeyModifier.ControlLeft, JsKeyModifier.ShiftLeft]" OnHotkeyPressed="() => _paymentModeAutoComplete?.FocusAsync()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.Enter KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="AddItemToCart" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyK KeyModifiers=[JsKeyModifier.AltLeft] OnHotkeyPressed="PrintKOT" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyS KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => SaveTransaction()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyT KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => SaveTransaction(saveThermal: true)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyP KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => SaveTransaction(savePDF: true)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyE KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => SaveTransaction(saveExcel: true)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyT KeyModifiers=[JsKeyModifier.AltLeft] OnHotkeyPressed="() => PrintThermalInvoice()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyP KeyModifiers=[JsKeyModifier.AltLeft] OnHotkeyPressed="() => ExportSelectedTransaction()" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyE KeyModifiers=[JsKeyModifier.AltLeft] OnHotkeyPressed="() => ExportSelectedTransaction(true)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyH KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => AuthenticationService.NavigateToRoute(RestaurantRouteNames.BillReport, FormFactor, JSRuntime, NavigationManager)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.KeyI KeyModifiers=[JsKeyModifier.ControlLeft] OnHotkeyPressed="() => AuthenticationService.NavigateToRoute(RestaurantRouteNames.BillItemReport, FormFactor, JSRuntime, NavigationManager)" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.Delete OnHotkeyPressed="RemoveSelectedCartItem" Disabled="@_isProcessing" />
<MudBlazor.MudHotkey Key=JsKey.Insert OnHotkeyPressed="EditSelectedCartItem" Disabled="@_isProcessing" />

@if (_isLoading || _user is null)
{
	<LoadingScreen />
}
else
{
	<div class="page-shell">
		<Header Title="@(_bill.Id > 0 ? $"Edit Bill {_bill.TransactionNo}" : "Create Bill")">
			<LeftContent>
				<MudBlazor.MudMenu Label="File" Dense="true" Disabled="_isProcessing">
					<MudBlazor.MudMenuItem Label="New Transaction (Ctrl + N)" OnClick="ResetPage" />
					<MudBlazor.MudMenuItem Label="Save & KOT (Alt + K)" OnClick="PrintKOT" Disabled="@(_isProcessing || !NeedsSave)" />
					<MudBlazor.MudDivider />
					<MudBlazor.MudMenuItem Label="Save (Ctrl + S)" OnClick="() => SaveTransaction()" />
					<MudBlazor.MudMenuItem Label="Save & Thermal (Ctrl + T)" OnClick="() => SaveTransaction(saveThermal: true)" />
					<MudBlazor.MudMenuItem Label="Save & PDF (Ctrl + P)" OnClick="() => SaveTransaction(savePDF: true)" />
					<MudBlazor.MudMenuItem Label="Save & Excel (Ctrl + E)" OnClick="() => SaveTransaction(saveExcel: true)" />
					<MudBlazor.MudDivider />
					<MudBlazor.MudMenuItem Label="Export Thermal (Alt + T)" OnClick="() => PrintThermalInvoice()" Disabled="@(_isProcessing || NeedsSave)" />
					<MudBlazor.MudMenuItem Label="Export PDF (Alt + P)" OnClick="() => ExportSelectedTransaction()" Disabled="@(_isProcessing || NeedsSave)" />
					<MudBlazor.MudMenuItem Label="Export Excel (Alt + E)" OnClick="() => ExportSelectedTransaction(true)" Disabled="@(_isProcessing || NeedsSave)" />
				</MudBlazor.MudMenu>

				<MudBlazor.MudMenu Label="Open" Dense="true" Disabled="_isProcessing">
					<MudBlazor.MudMenuItem Label="Dining Dashboard" OnClick="() => AuthenticationService.NavigateToRoute(RestaurantRouteNames.DiningDashboard, FormFactor, JSRuntime, NavigationManager)" />
					<MudBlazor.MudMenuItem Label="Bill Report (Ctrl + H)" OnClick="() => AuthenticationService.NavigateToRoute(RestaurantRouteNames.BillReport, FormFactor, JSRuntime, NavigationManager)" />
					<MudBlazor.MudMenuItem Label="Bill Item Report (Ctrl + I)" OnClick="() => AuthenticationService.NavigateToRoute(RestaurantRouteNames.BillItemReport, FormFactor, JSRuntime, NavigationManager)" />
				</MudBlazor.MudMenu>
			</LeftContent>
		</Header>

		<div class="page-container">

			<div class="three-column-layout">

				<!-- Transaction -->
				<div class="section-card">
					<div class="section-header">
						<h2 class="section-title">Transaction</h2>
					</div>

					<div class="section-body">
						<div class="form-grid">

							@if (_user.LocationId == 1)
							{
								<!-- Company -->
								<div class="form-field">
									<CustomAutoComplete @ref="_firstFocus" T="CompanyModel"
														Value="_selectedCompany"
														DataSource="@_companies"
														Placeholder="Select a Company"
														Label="Company (Ctrl + F)"
														ValueChanged="OnCompanyChanged"
														FieldValue="@nameof(CompanyModel.Name)"
														AddNewRoute="@AccountsRouteNames.CompanyMaster" />
								</div>

								<!-- Location -->
								<div class="form-field">
									<CustomAutoComplete T="LocationModel"
														Value="_selectedLocation"
														DataSource="@_locations"
														Placeholder="Select a Location"
														Label="Location"
														ValueChanged="OnLocationChanged"
														FieldValue="@nameof(LocationModel.Name)"
														AddNewRoute="@OperationRouteNames.Location" />
								</div>

								<!-- Transaction Date -->
								<div class="form-field">
									<CustomDatePicker @bind-Value="_bill.TransactionDateTime"
													  FinancialYear="@_selectedFinancialYear" />
								</div>
							}

							<!-- Transaction No -->
							<div class="form-field">
								<CustomTextField @bind-Value="_bill.TransactionNo"
												 Label="Transaction No"
												 Placeholder="Transaction No"
												 ReadOnly="true"
												 TabIndex="-1" />
							</div>

							<!-- Dining Table -->
							<div class="form-field">
								<CustomAutoComplete T="DiningTableModel"
													Value="_selectedDiningTable"
													DataSource="@_diningTables"
													Placeholder="Select a Dining Table"
													Label="Dining Table"
													ValueChanged="OnDiningTableChanged"
													FieldValue="@nameof(DiningTableModel.Name)"
													AddNewRoute="@RestaurantRouteNames.DiningTable" />
							</div>

							<!-- Customer Number -->
							<div class="form-field">
								<CustomTextField Value="@_selectedCustomer.Number"
												 ValueChanged="OnCustomerNumberChanged"
												 Label="Customer Number"
												 Placeholder="Customer Mobile No"
												 Required="false"
												 AddNewRoute="@StoreRouteNames.Customer"
												 InputType="MudBlazor.InputType.Telephone" />
							</div>

							<!-- Customer Name -->
							<div class="form-field">
								<CustomTextField @bind-Value="_selectedCustomer.Name"
												 Label="Customer Name"
												 Placeholder="Customer Name"
												 Required="false"
												 AddNewRoute="@StoreRouteNames.Customer" />
							</div>

							<!-- Total People -->
							<div class="form-field">
								<CustomNumericField T="int"
													Value="_bill.TotalPeople"
													ValueChanged="OnTotalPeopleChanged"
													Label="Total People"
													Placeholder="1"
													Min="1"
													Max="100" />
							</div>

							<!-- Remarks -->
							<div class="form-field form-field-full">
								<CustomTextField @bind-Value="_bill.Remarks"
												 Label="Remarks / Narration"
												 Placeholder="Enter remarks..."
												 Required="false"
												 Lines="2" />
							</div>
						</div>
					</div>
				</div>

				<!-- Financial -->
				<div class="section-card">
					<div class="section-header">
						<h2 class="section-title">Financial</h2>
					</div>

					<div class="section-body">
						<div class="form-grid">

							<!-- Sub Total -->
							<div class="form-field">
								<CustomNumericField T="decimal"
													Value="_bill.TotalAfterTax"
													Label="Sub Total"
													Placeholder="0.00"
													ReadOnly="true"
													Required="false"
													TabIndex="-1" />
							</div>

							@if (_user.ChangeProductFinancial)
							{
								<!-- Discount -->
								<div class="form-field">
									<div class="date-year-wrapper">
										<CustomNumericField @ref="_discountPercentField" T="decimal"
															Value="_bill.DiscountPercent"
															ValueChanged="OnDiscountPercentChanged"
															Label="Disc % (Ctrl + Shift + F)"
															Placeholder="0.00"
															Required="false"
															ReadOnly="@(!_user.ChangeProductFinancial)"
															Min="0"
															Max="100" />

										<CustomNumericField T="decimal"
															Value="_bill.DiscountAmount"
															Label="Discount Amt"
															Placeholder="0.00"
															ReadOnly="true"
															Required="false"
															TabIndex="-1" />
									</div>
								</div>

								<!-- Service Charge -->
								<div class="form-field">
									<div class="date-year-wrapper">
										<CustomNumericField T="decimal"
															Value="_bill.ServiceChargePercent"
															ValueChanged="OnServiceChargePercentChanged"
															Label="Service Charge %"
															Placeholder="0.00"
															Required="false"
															ReadOnly="@(!_user.ChangeProductFinancial)"
															Min="0"
															Max="100" />

										<CustomNumericField T="decimal"
															Value="_bill.ServiceChargeAmount"
															Label="Service Charge Amt"
															Placeholder="0.00"
															ReadOnly="true"
															Required="false"
															TabIndex="-1" />
									</div>
								</div>
							}

							<!-- Round Off -->
							<div class="form-field">
								<CustomNumericField T="decimal"
													Value="_bill.RoundOffAmount"
													ValueChanged="OnRoundOffAmountChanged"
													Label="Round Off"
													Placeholder="0.00"
													Required="false"
													ReadOnly="@(!_user.ChangeProductFinancial)" />
							</div>

							<hr class="form-divider" />

							<!-- Total Amount -->
							<div class="form-field">
								<CustomNumericField T="decimal"
													Value="_bill.TotalAmount"
													Label="Total Amount"
													Placeholder="0.00"
													ReadOnly="true"
													Required="false"
													TabIndex="-1" />
							</div>
						</div>
					</div>
				</div>

				<!-- Payment -->
				<div class="section-card">
					<div class="section-header">
						<h2 class="section-title">Payment</h2>
					</div>

					<div class="section-body">
						<div class="cart-input-row">
							<!-- Payment Mode -->
							<div class="cart-field cart-field-wide">
								<CustomAutoComplete T="PaymentModeModel"
													@ref="_paymentModeAutoComplete"
													FieldValue="@nameof(PaymentModeModel.Name)" Value="_selectedPaymentMethod"
													DataSource="@_paymentMethods" Placeholder="Select Payment Mode"
													ValueChanged="OnPaymentMethodChanged" OpenOnFocus="false"
													Label="Payment Mode (Ctrl + Shift + G)" />
							</div>

							<!-- Amount -->
							<div class="cart-field cart-field-wide">
								<CustomNumericField T="decimal"
													@bind-Value="_selectedPaymentCart.Amount"
													Min="0"
													Label="Amount"
													Placeholder="0.00" />
							</div>

							<!-- Add Button -->
							<div class="cart-field cart-field-wide">
								<IconButton Icon="IconType.Add" Variant="ButtonVariant.Add" Text="Add"
											Title="Add Payment" OnClick="AddPaymentToCart" />
							</div>
						</div>

						<SfGrid @ref="_sfPaymentsCartGrid" DataSource="_paymentsCart"
								AllowSorting="true" AllowResizing="true"
								AllowFiltering="true" AllowReordering="true" ShowColumnChooser="true"
								GridLines="GridLine.Horizontal" ContextMenuItems="@_paymentsCartGridContextMenuItems" CssClass="professional-grid">

							<GridEvents TValue="PaymentItem" ContextMenuItemClicked="OnPaymentsCartGridContextMenuItemClicked" />
							<GridFilterSettings Type="Syncfusion.Blazor.Grids.FilterType.Excel" />

							<GridAggregates>
								<GridAggregate>
									<GridAggregateColumns>
										<GridAggregateColumn Field="@nameof(PaymentItem.Method)" Type="AggregateType.Count">
											<FooterTemplate>
												@{
													var aggregate = (context as AggregateTemplateContext);
												}
												<div class="aggregate-label">
													<strong>Total Payment Modes: @aggregate.Count</strong>
												</div>
											</FooterTemplate>
										</GridAggregateColumn>
										<GridAggregateColumn Field="@nameof(PaymentItem.Amount)" Type="AggregateType.Sum">
											<FooterTemplate>
												@{
													var aggregate = (context as AggregateTemplateContext);
													var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
												}
												<div class="aggregate-value aggregate-total">
													<strong>@sum.ToString("N2")</strong>
												</div>
											</FooterTemplate>
										</GridAggregateColumn>
									</GridAggregateColumns>
								</GridAggregate>
							</GridAggregates>

							<GridColumns>
								<GridColumn Field="@nameof(PaymentItem.Method)" HeaderText="Payment Mode" Width="200" />
								<GridColumn Field="@nameof(PaymentItem.Amount)" HeaderText="Amount" Width="130" TextAlign="TextAlign.Right" Format="N2">
									<Template>
										@{
											var item = (context as dynamic);
											<span class="grid-total">@item.Amount.ToString("N2")</span>
										}
									</Template>
								</GridColumn>
							</GridColumns>

						</SfGrid>
					</div>
				</div>
			</div>

			<!-- Add to Cart -->
			<div class="section-card">
				<div class="section-header">
					<h2 class="section-title">Add Items to Cart</h2>
				</div>

				<div class="section-body">
					<!-- First Row: Item Selection and Tax Info -->
					<div class="cart-input-row">

						<!-- Item -->
						<div class="cart-field cart-field-wide">
							<CustomAutoComplete T="ProductLocationOverviewModel"
												@ref="_itemAutoComplete"
												Value="_selectedProduct"
												DataSource="@_products"
												Placeholder="Select a Product"
												Label="Product (Ctrl + G)"
												ValueChanged="OnItemChanged"
												FieldValue="@nameof(ProductLocationOverviewModel.Name)"
												AddNewRoute="@StoreRouteNames.Product"
												OpenOnFocus="false" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.Quantity"
												ValueChanged="OnItemQuantityChanged"
												Label="Qty"
												Placeholder="0.00"
												Min="0" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.Rate"
												ValueChanged="OnItemRateChanged"
												Label="Rate"
												Placeholder="0.00"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												Min="0" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.DiscountPercent"
												ValueChanged="OnItemDiscountPercentChanged"
												Label="Disc %"
												Placeholder="0.00"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												Required="false"
												Min="0"
												Max="100" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.CGSTPercent"
												ValueChanged="OnItemCGSTPercentChanged"
												Label="CGST %"
												Placeholder="0.00"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												Required="false"
												Min="0"
												Max="100" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.SGSTPercent"
												ValueChanged="OnItemSGSTPercentChanged"
												Label="SGST %"
												Placeholder="0.00"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												Required="false"
												Min="0"
												Max="100" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.IGSTPercent"
												ValueChanged="OnItemIGSTPercentChanged"
												Label="IGST %"
												Placeholder="0.00"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												Required="false"
												Min="0"
												Max="100" />
						</div>

						<div class="cart-field">
							<div class="checkbox-wrapper">
								<CustomCheckBox Value="_selectedCart.InclusiveTax"
												ReadOnly="@(!_user.ChangeProductFinancial)"
												ValueChanged="OnItemInclusiveTaxChanged"
												Label="Tax Incl." />
							</div>
						</div>

						<div class="cart-field" />
					</div>

					<!-- Second Row: Readonly Amounts -->
					<div class="cart-input-row">
						<div class="cart-field cart-field-wide">
							<CustomTextField @bind-Value="_selectedCart.Remarks"
											 Label="Remarks"
											 Placeholder="Item Narration..."
											 Required="false" />
						</div>

						<div class="cart-field" />

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.BaseTotal"
												Label="Base"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.DiscountAmount"
												Label="Disc Amt"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.CGSTAmount"
												Label="CGST Amt"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.SGSTAmount"
												Label="SGST Amt"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.IGSTAmount"
												Label="IGST Amt"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field">
							<CustomNumericField T="decimal"
												Value="_selectedCart.Total"
												Label="Total"
												ReadOnly="true"
												Required="false"
												TabIndex="-1" />
						</div>

						<div class="cart-field cart-field-action">
							<IconButton Icon="IconType.Add" Variant="ButtonVariant.Add" Text="Add" Title="Add to Cart" OnClick="AddItemToCart" />
						</div>
					</div>
				</div>
			</div>

			<!-- Cart -->
			<div class="section-card">
				<div class="section-header">
					<h2 class="section-title">Cart</h2>
				</div>

				<div class="section-body grid-body">
					<SfGrid @ref="_sfCartGrid" DataSource="_cart"
							AllowSorting="true" AllowResizing="true"
							AllowFiltering="true" AllowReordering="true" ShowColumnChooser="true"
							GridLines="GridLine.Horizontal" ContextMenuItems="@_cartGridContextMenuItems" CssClass="professional-grid"
							Toolbar="@(new List<string>() { "ColumnChooser", "Search" })">

						<GridEvents TValue="BillItemCartModel" ContextMenuItemClicked="OnCartGridContextMenuItemClicked" />
						<GridFilterSettings Type="Syncfusion.Blazor.Grids.FilterType.Excel" />

						<GridAggregates>
							<GridAggregate>
								<GridAggregateColumns>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.ItemName)" Type="AggregateType.Count">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
											}
											<div class="aggregate-label">
												<strong>Total Items: @aggregate.Count</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.Quantity)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.BaseTotal)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.DiscountAmount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value aggregate-debit">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.AfterDiscount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.CGSTAmount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.SGSTAmount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.IGSTAmount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.TotalTaxAmount)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
									<GridAggregateColumn Field="@nameof(BillItemCartModel.Total)" Type="AggregateType.Sum">
										<FooterTemplate>
											@{
												var aggregate = (context as AggregateTemplateContext);
												var sum = aggregate.Sum != null ? Convert.ToDecimal(aggregate.Sum) : 0m;
											}
											<div class="aggregate-value aggregate-total">
												<strong>@sum.ToString("N2")</strong>
											</div>
										</FooterTemplate>
									</GridAggregateColumn>
								</GridAggregateColumns>
							</GridAggregate>
						</GridAggregates>

						<GridColumns>
							<GridColumn Field="@nameof(BillItemCartModel.ItemName)" HeaderText="Item Name" Width="200" />
							<GridColumn Field="@nameof(BillItemCartModel.Quantity)" HeaderText="Qty" Width="100" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.Rate)" HeaderText="Rate" Width="110" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.BaseTotal)" HeaderText="Base Total" Width="150" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.DiscountPercent)" HeaderText="Disc %" Width="120" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.DiscountAmount)" HeaderText="Disc Amt" Width="140" TextAlign="TextAlign.Right" Format="N2">
								<Template>
									@{
										var item = (context as dynamic);
										<span class="grid-debit">@item.DiscountAmount.ToString("N2")</span>
									}
								</Template>
							</GridColumn>
							<GridColumn Field="@nameof(BillItemCartModel.AfterDiscount)" HeaderText="After Disc" Width="140" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.CGSTPercent)" HeaderText="CGST %" Width="140" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.CGSTAmount)" HeaderText="CGST Amt" Width="150" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.SGSTPercent)" HeaderText="SGST %" Width="140" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.SGSTAmount)" HeaderText="SGST Amt" Width="150" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.IGSTPercent)" HeaderText="IGST %" Width="140" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.IGSTAmount)" HeaderText="IGST Amt" Width="150" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.TotalTaxAmount)" HeaderText="Tax Total" Width="150" TextAlign="TextAlign.Right" Format="N2" />
							<GridColumn Field="@nameof(BillItemCartModel.InclusiveTax)" HeaderText="Tax Incl." Width="100" TextAlign="TextAlign.Center" DisplayAsCheckBox="true" />
							<GridColumn Field="@nameof(BillItemCartModel.KOTPrint)" HeaderText="KOT" Width="90" TextAlign="TextAlign.Center" DisplayAsCheckBox="true" />
							<GridColumn Field="@nameof(BillItemCartModel.Total)" HeaderText="Total" Width="130" TextAlign="TextAlign.Right" Format="N2">
								<Template>
									@{
										var item = (context as dynamic);
										<span class="grid-total">@item.Total.ToString("N2")</span>
									}
								</Template>
							</GridColumn>
							<GridColumn Field="@nameof(BillItemCartModel.Remarks)" HeaderText="Remarks" Width="150" />
						</GridColumns>
					</SfGrid>
				</div>
			</div>
		</div>

		<Footer />
	</div>
}

<ToastNotification @ref="_toastNotification" />

<CloseGuard Block="_isProcessing" />
