CREATE PROCEDURE [dbo].[Delete_TableData]
	@TableName VARCHAR(50)
AS
BEGIN
	SET NOCOUNT ON;

	DECLARE @SQL NVARCHAR(MAX);
	SET @SQL = N'WHILE 1 = 1 BEGIN DELETE TOP (10000) FROM ' + QUOTENAME(@TableName) + N'; IF @@ROWCOUNT = 0 BREAK; END';
	EXEC [sys].[sp_executesql] @SQL;
END